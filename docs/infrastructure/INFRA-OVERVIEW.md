# Topologia de rede, VPCs, escalabilidade

```sh
├── diagrams/           # Diagramas de infra (draw.io, PlantUML, etc.)
├── terraform/          # Scripts Terraform
├── helm/               # Charts do Kubernetes
└── ansible/            # Playbooks (se usados)
```